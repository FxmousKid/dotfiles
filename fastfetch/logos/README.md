# fastfetch / logos

Custom text logos for Fastfetch.

## Files

- `apple-purple.txt` — the logo used by `../config.jsonc`.

## Notes

The config expects it at `~/.config/fastfetch/logos/apple-purple.txt`.
