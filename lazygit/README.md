# lazygit

Lazygit config.

## Files

- `config.yml` — sets the editor to `lvim` and themes the UI.

## Notes

Colors match the Tokyo Night look used elsewhere (purple active borders,
blue/coral/yellow accents).
