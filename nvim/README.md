# nvim

A small standalone Neovim config.

## Files

- `init.lua` — indentation, line numbers, autoindent, leader key, `whichwrap`.

## Notes

Separate from LunarVim (see `lvim/`). This is the plain-nvim fallback.
