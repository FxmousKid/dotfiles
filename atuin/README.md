# atuin

Config for Atuin (shell history search).

## Files

- `config.toml` — mostly defaults, plus: full-screen UI, Enter runs the picked
  command, and `vim-normal` keymap at startup.

## Notes

The shell hooks live in the shell configs (`eval "$(atuin init zsh)"`, same for bash).
